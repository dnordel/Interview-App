python-docx==1.2.0
soundfile==0.13.1
faster-whisper==1.2.1
tkcalendar==1.6.1
transformers==4.52.4

# Local DeepSeek uses Ollama, installed and model-pulled by setup_and_run.ps1.
# No Python client package is required; the app uses Ollama's local OpenAI-compatible HTTP endpoint.

# GPU runtime add-ons for faster-whisper (CUDA 12 stack)
ctranslate2>=4.5.0
nvidia-cublas-cu12
nvidia-cudnn-cu12
