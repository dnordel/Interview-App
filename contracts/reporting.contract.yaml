module:
  name: reporting
  language: python
  path: src/reporting.py
  version: 1.2.2
  description: Deprecated compatibility wrapper for scoring, draft persistence, and report document export services implemented by scoring_reporting.
functions: []
classes:
- name: ReportingValidationError
  description: Domain validation error raised during reporting/export operations.
  locked: false
  attributes: {}
  methods: []
- name: ScoringEngine
  description: Static scoring workflow for interview trait evaluations.
  locked: false
  attributes: {}
  methods:
  - name: _coerce_raw_score
    description: Coerce raw score candidate values into bounded integer values.
    locked: false
    inputs:
      value:
        type: Any
        required: true
    returns:
      type: tuple[Optional[int], int]
    exceptions: []
    side_effects: none
  - name: _get_track_config
    description: Resolve and validate track configuration from rubric.
    locked: false
    inputs:
      rubric:
        type: dict[str, Any]
        required: true
      track_key:
        type: Any
        required: true
    returns:
      type: dict[str, Any]
    exceptions:
    - ReportingValidationError
    side_effects: none
  - name: _resolve_track_key_for_scoring
    description: Resolve rubric track key from operator-selected track identifier.
    locked: false
    inputs:
      rubric:
        type: dict[str, Any]
        required: true
      track_key:
        type: Any
        required: true
    returns:
      type: str
    exceptions:
    - ReportingValidationError
    side_effects: none
  - name: _calculate_percent
    description: Compute precise and rounded percentages.
    locked: false
    inputs:
      weighted_total:
        type: int
        required: true
      denominator:
        type: int
        required: true
    returns:
      type: tuple[Optional[Decimal], float]
    exceptions: []
    side_effects: none
  - name: _is_critical_priority
    description: Determine whether a trait priority should be treated as critical.
    locked: false
    inputs:
      priority:
        type: Any
        required: true
    returns:
      type: bool
    exceptions: []
    side_effects: none
  - name: evaluate
    description: Evaluate scoring rows, aggregate totals, and compute hiring outcome using skipped-aware determination thresholds.
    locked: false
    inputs:
      rubric:
        type: dict[str, Any]
        required: true
      track_key:
        type: Any
        required: true
      trait_results:
        type: dict[str, dict[str, Any]]
        required: true
    returns:
      type: dict[str, Any]
    exceptions:
    - ReportingValidationError
    side_effects: none
- name: DraftManager
  description: JSON draft persistence for interview sessions.
  locked: false
  attributes: {}
  methods:
  - name: __init__
    description: Initialize draft/final output directories.
    locked: false
    inputs:
      base_dir:
        type: Path
        required: true
    returns:
      type: Any
    exceptions: []
    side_effects: Creates draft and final directories.
  - name: save_draft
    description: Persist draft payload to timestamped JSON file.
    locked: false
    inputs:
      payload:
        type: dict[str, Any]
        required: true
    returns:
      type: Path
    exceptions:
    - OSError
    side_effects: Writes draft file to disk.
  - name: load_draft
    description: Load draft payload from JSON file.
    locked: false
    inputs:
      path:
        type: Path
        required: true
    returns:
      type: dict[str, Any]
    exceptions:
    - OSError
    - json.JSONDecodeError
    side_effects: Reads draft file from disk.
- name: DocxExporter
  description: DOCX report exporter for finalized interview artifacts.
  locked: false
  attributes: {}
  methods:
  - name: __init__
    description: Initialize report output directory.
    locked: false
    inputs:
      output_dir:
        type: Path
        required: true
    returns:
      type: Any
    exceptions: []
    side_effects: Creates output directory.
  - name: _require_candidate
    description: Validate payload candidate object shape.
    locked: false
    inputs:
      payload:
        type: dict[str, Any]
        required: true
    returns:
      type: dict[str, Any]
    exceptions:
    - ReportingValidationError
    side_effects: none
  - name: _require_candidate_field
    description: Validate required candidate field and return normalized value.
    locked: false
    inputs:
      candidate:
        type: dict[str, Any]
        required: true
      field_name:
        type: str
        required: true
    returns:
      type: str
    exceptions:
    - ReportingValidationError
    side_effects: none
  - name: _extract_full_candidate_transcript
    description: Collect full candidate transcript preferring per-flow Whisper transcript text.
    locked: false
    inputs:
      payload:
        type: dict[str, Any]
        required: true
    returns:
      type: str
    exceptions: []
    side_effects: none
  - name: export
    description: Generate finalized interview report document through scoring_reporting, preserving the director-ready DOCX layout with bulleted executive strengths/concerns, a compact scorecard before answer summaries, compact question/rating/summary cards that use exact question/prompt text, consolidate Interviewer/AI-Advisor direct-model/AI-trait-based signal-weighted ratings into one row, and omit consolidated evidence rows plus non-scored rubric-alignment rows, full transcript detail in appendices, no hiring-manager evidence-notes section, no duplicated answer-summary evidence in the transcript appendix, and no interviewer-adjustment rows in the AI advisory appendix.
    locked: false
    inputs:
      rubric:
        type: dict[str, Any]
        required: true
      payload:
        type: dict[str, Any]
        required: true
      scoring:
        type: dict[str, Any]
        required: true
    returns:
      type: Path
    exceptions:
    - ReportingValidationError
    - OSError
    side_effects: Writes DOCX report file to disk.
data_structures: []
dependencies:
- scoring_reporting
