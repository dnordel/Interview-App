module:
  name: interview_app_session_context
  language: python
  path: src/interview_app/session_context.py
  version: 1.0.0
  description: Centralized session identity derivation and runtime path policy helpers for interview runtime setup.
functions: []
classes:
- name: InterviewSessionContext
  description: Provides sanitized naming, session key composition, and runtime path/log helpers.
  locked: false
  attributes: {}
  methods:
  - name: __init__
    description: Initializes runtime context providers and path anchors.
    locked: false
    inputs:
      app_root:
        type: Path
        required: true
      default_base_dir:
        type: Path
        required: true
      today_provider:
        type: Callable[[], date] | None
        required: false
      now_provider:
        type: Callable[[], datetime] | None
        required: false
    returns:
      type: None
    exceptions: []
    side_effects: none
  - name: safe_interview_date
    description: Returns an explicit interview date or a current-date fallback.
    locked: false
    inputs:
      raw_date:
        type: str
        required: true
    returns:
      type: str
    exceptions: []
    side_effects: none
  - name: safe_base_name
    description: Builds a filesystem-safe interview base name from candidate and date values.
    locked: false
    inputs:
      candidate_name:
        type: str
        required: true
      interview_date:
        type: str
        required: true
    returns:
      type: str
    exceptions: []
    side_effects: none
  - name: active_session_key
    description: Composes deterministic session key tuple values for session storage.
    locked: false
    inputs:
      interview_session_id:
        type: str
        required: true
      candidate_name:
        type: str
        required: true
      interview_date:
        type: str
        required: true
    returns:
      type: tuple[str, str, str]
    exceptions: []
    side_effects: none
  - name: validate_runtime_base_dir
    description: Resolves and validates base directory policy with optional write-probe.
    locked: false
    inputs:
      raw_base_dir:
        type: str
        required: true
      write_probe:
        type: bool
        required: false
        default: true
    returns:
      type: Path
    exceptions:
    - OSError
    side_effects: creates base directory and performs write probe when enabled
  - name: runtime_init_log_path
    description: Returns deterministic runtime initialization log path under app logs.
    locked: false
    inputs: {}
    returns:
      type: Path
    exceptions: []
    side_effects: none
data_structures: []
dependencies: []
